int main() {
	int X = 3;
	int Y = 2;
	int Z = 4;
	Y = (X + Y) - Z;
	}
}

// http://www.assis.pro.br/public_html/davereed/14-DentroDoComputador.html
// http://www.assis.pro.br/public_html/davereed/KandS/machine.html

LOAD R0 5	// carrega posicao de memoria 5 (X) em R0
LOAD R1 6	// carrega posicao de memoria 6 (Y) em R1
ADD R2 R0 R1	// adiciona R0 e R1, armazena em R2
LOAD R3 7	// carrega posicao de memoria 7 (Z) em R3
SUB R1 R2 R3	// subtrai R2 de R3, armazena em R1
STORE 6 R2	// armazena R1 na posicao de memoria 6 (Y)
HALT		// parada
3		// X, dado a ser somado
2		// Y, dado a ser somado
4		// Z, dado a ser subtraído


